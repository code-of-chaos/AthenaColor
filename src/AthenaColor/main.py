# ----------------------------------------------------------------------------------------------------------------------
# - Package Imports -
# ----------------------------------------------------------------------------------------------------------------------
# General Packages
import sys
import time

# Custom Library

# Custom Packages
from AthenaColor.Help.PrintOut import AllFore,AllTable,AllTable_UnverfifiedPycharm,AllBoxes

# ----------------------------------------------------------------------------------------------------------------------
# - Code -
# ----------------------------------------------------------------------------------------------------------------------
if __name__ == "__main__":
    # sys.stdout.write(f"{esc}[2560u")
    import AthenaColor.Help.readme
    # sys.stdout.write("this is a test")
    # # sys.stdout.flush()
    #
    # # sys.stdout.buffer.write(f"{esc}[G".encode())
    # # sys.stdout.flush()
    #
    # print()
    #
    # for i in range(5):
    #     sys.stdout.flush()
    #     sys.stdout.write(str(i))
    #     sys.stdout.flush()
    #     time.sleep(0.2)
    #     sys.stdout.write(f"{esc}[0J")
    #     sys.stdout.flush()
    #     sys.stdout.write("\b")
    #     sys.stdout.flush()
    #
    #
    # sys.stdout.flush()
